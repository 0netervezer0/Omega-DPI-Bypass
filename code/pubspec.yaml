name: omega_dpi_bypass
description: Deep Packet Inspection Bypassing Tool For Windows
version: 2.1.0

environment:
  sdk: '>=2.19.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  window_manager: ^0.2.0
  url_launcher: ^6.1.0
  flutter_launcher_icons: ^0.13.1

dev_dependencies:
  flutter_launcher_icons: ^0.13.1

flutter:
  uses-material-design: true
  assets:
    - assets/icons/app_icon.ico

flutter_icons:
  windows: true
  image_path: "assets/icons/app_icon.png"
  image_path_windows: "assets/icons/app_icon.ico"